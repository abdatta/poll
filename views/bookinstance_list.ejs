<h1><%= title%></h1>
<% if(bookinstance_list.length == 0){ %>
<p>There are no book copies in this library.</p>
<% } else{ %>
    <ul>
        <% bookinstance_list.forEach(function(copy){ %>
            <li><a href='<%=copy.url%>' >
                <%=copy.book.title%> : <%=copy.imprint%> -
                <% if(copy.status='Available'){ %>
                    <span class="text-success"><%=copy.status%></span>
                <% }else if(copy.status='Maintenance'){ %>
                    <span class="text-danger"><%=copy.status%></span>
                <% }else{ %>
                    <span class="text-warning"><%=copy.status%></span>
                <% } if(copy.status!='Available'){ %>
                    <span> (Due: <%=copy.due_back%>)</span>
                <% } %>
            </a></li>
        <% }); %>
    </ul>
<% } %>