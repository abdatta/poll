<h1><%= title%></h1>
<% if(author_list.length == 0){ %>
<p>There are no authors.</p>
<% } else{ %>
    <ul>
        <% author_list.forEach(function(author){ %>
            <li><a href='<%=author.url%>' ><%=author.name%> | <%=author.dob%> - <%=author.dod%></a></li>
        <% }); %>
    </ul>
<% } %>